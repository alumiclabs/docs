# Logos

The files in this directory (`light.svg`, `dark.svg`) are the Mintlify starter-kit placeholders. Replace them with the Alumic brand mark when available. Keep both a light-mode and a dark-mode variant; the correct one is chosen by `docs.json → logo.{light,dark}`.

`/favicon.svg` at the docs root is a separate placeholder — replace alongside the logos.
